#ifndef UTILS_H
#define UTILS_H

#include <string>

// Placeholder for compression utilities (already implemented in hsmdif.cpp)
std::string compress(const std::string& data);
std::string decompress(const std::string& data);

#endif